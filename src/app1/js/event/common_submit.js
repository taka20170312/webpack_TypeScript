/**
 * @file app1<br>
 *       common_submit.js<br>
 *       レコード保存実行前の処理
 * @author INET
 */

const func = {
  /**
   * レコード保存のタイミングで実行するプログラム<br>
   * @function
   * @memberof app1
   * @param {Object} event イベントオブジェクト
   * @returns {Object} event イベントオブジェクト
   */
  exec(event) {
    return event;
  }
};

export default func;

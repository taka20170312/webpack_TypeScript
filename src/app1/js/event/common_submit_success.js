/**
 * @file app1<br>
 *       common_submit_success.js<br>
 *       レコード保存実行後の処理
 * @author INET
 */

const func = {
  /**
   * レコード保存後タイミングで実行するプログラム<br>
   * @function
   * @memberof app1
   * @param {Object} event イベントオブジェクト
   * @returns {Object} event イベントオブジェクト
   */
  exec(event) {
    return event;
  }
};

export default func;
